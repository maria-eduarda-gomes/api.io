<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chamada de API</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f4f9;
      color: #333;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 10px;
      color: #1a1a1a;
    }

    p {
      font-size: 1rem;
      color: #666;
      margin-bottom: 30px;
    }

    .button-container {
      display: flex;
      gap: 20px;
    }

    button {
      padding: 15px 20px;
      font-size: 1rem;
      font-weight: bold;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s;
      width: 160px;
    }

    button:focus {
      outline: none;
    }

    button:hover {
      transform: translateY(-3px);
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .blue-button {
      background-color: #007bff;
      color: #fff;
    }

    .green-button {
      background-color: #28a745;
      color: #fff;
    }
  </style>
</head>
<body>
  <h1>Chamada de API</h1>
  <p>Clique em um dos botões para começar a consulta</p>
  <div class="button-container">
    <button class="blue-button" onclick="callApi()">Chamar API</button>
    <button class="green-button" onclick="listCuriosities()">Listagem de curiosidades</button>
  </div>

  <script>
    function callApi() {
      alert('API chamada!');
      // Lógica para chamar API
    }

    function listCuriosities() {
      alert('Listando curiosidades!');
      // Lógica para listar curiosidades
    }
  </script>
</body>
</html>
